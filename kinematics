using System;
using static System.Console;

namespace calc
{
    internal class Kinematics
    {
        public static void VariableAcceleration()
        {
            WriteLine("Are you trying to work out distance, velocity or acceleration?");
            if (ReadLine() == "distance")
            {
                WriteLine("What do you currently have?");
                if (ReadLine() == "velocity")
                {
                    WriteLine(
                        "To workout distance from velocity, we have to integrate once, please enter your velocity");
                    var v = Convert.ToInt32(ReadLine());
                }
            }
        }

        public void ConstantAcceleration()
        {
        }

        public void ConstantVelocity()
        {
        }
    }
}
